# MySQL connection used by Prisma (packages/db) — shared by apps/api and
# apps/web's Better-auth instance. Point every app's own .env at the same URL.
DATABASE_URL="mysql://root:your_password_here@localhost:3306/sgs"
